# .env
# Copy this file and fill in your actual PostgreSQL credentials.
# Never commit the real .env to GitHub — add it to .gitignore!

DB_HOST=localhost
DB_NAME=kayakonnect
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_PORT=5433